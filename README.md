
# OCaml - Knight's Tour

## Usage

`Compile` the 2 source files into 1 using the following command:

```
ocamlopt -o kt knight_tour.ml successori.ml
```

Then start a knight's tour with `kt 'startingPosition' 'chessboardDimension'`, like for example:

```
kt (1,1) 5
```
